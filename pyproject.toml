[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ollama-agent"
version = "0.1.0"
description = "Autonomous support agent with Ollama"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
